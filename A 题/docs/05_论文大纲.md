# 论文骨架(五校联赛 A 题;数字待结果填充后定稿)

## 标题
低空经济背景下多无人机协同巡检路径优化:下界驱动的 min-makespan 调度、负载均衡与禁飞区规避

## 摘要(模板)
针对…问题,建立…模型:①以总工时下界与构造性调度共同确定理论最小无人机数 Nmin(4 算例分别为 _/_/_/_);②以 min-max VRP 求 makespan 最短的任务分配与路径;③用分层目标规划(先 makespan 后 δ)实现负载均衡;④以"切线绕行+时间窗等待+迭代修复"处理临时禁飞区,全部调度经独立校验零违例。敏感性分析表明…。

## 1 问题重述与分析
- 1.1 背景与任务:基地(0,0)、速度 55 km/h、单次巡检 5 min、1 单位=100 m、等级 I/II/III → 3/2/1 次。
- 1.2 三问关系与依赖图(见 docs/01)。
- 1.3 假设清单 A1–A5 与依据(见 docs/01、docs/03 第 2 节)。

## 2 数据说明与预处理
- 附件1:4 算例共 381 点,等级分布、坐标范围(表,来自 docs/02)。
- 附件2:21 个禁飞区,时间窗全部落在 8:00–17:00;Case4 Z8 零长度窗口处理。
- 审计结论:ID 连续、无重复点、无非法值;无需清洗。

## 3 问题1 建模与求解
- 3.1 符号表(docs/03 §1)。
- 3.2 理论下界:总工时分解、LB1–LB5 推导与严格性;N_LB 表;证明结构("下界+构造")。
- 3.3 min-makespan VRP 模型与 GLS 算法(参数、种子、时限)。
- 3.4 结果:表2(Nmin、Tmax、Tmin);Nmin-1 不可行证据;证明缺口声明。
- 3.5 验证:独立重算校验通过;makespan 下界对照;路径图/甘特图。

## 4 问题2 建模与求解
- 4.1 双目标与分层(ε-约束)策略。
- 4.2 下界 L 二分搜索算法(单调性引理 + oracle 机制 + 粗网格对照)。
- 4.3 结果:表3 + Pareto 剖面(ε=0/1%/2%);与问题1 基线对比;δ 下界证书。

## 5 问题3 建模与求解
- 5.1 禁飞区约束形式化(开圆约定、时间窗、等待语义)。
- 5.2 修复式算法(双切线绕行/等待)+ 带时间窗再优化(Pass A/B)。
- 5.3 结果:表4 + 零违例声明 + 绕行/等待统计;禁飞区叠加路径图。

## 6 模型评价与敏感性分析
- 6.1 优势:证据可追溯、校验独立、算法通用。
- 6.2 局限:欧氏平面假设、GLS 为近似最优、下界对 N≥2 宽松、等待/悬停能力假设。
- 6.3 敏感性:S1 速度、S2 服务时长、S3 半径膨胀、S4 Z8 解读、S5 强制间隔、S6 任务扰动(表+一句结论)。

## 7 结论与建议
- Nmin/Tmax/δ 汇总表;调度方案交付说明(result1-3.xlsx);对运营者的建议(机型、冗余、动态重规划)。

## 附:可复现性
- 依赖、种子、命令、输出路径见 README_运行手册.md;代码 src/;机器可读结果 output/。

> 撰写规范(SKILL.md 最终审计):覆盖度/可追溯/数学符号/数据/代码/验证/图表/写作八项全过再交付。
